#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

void inicializarJogo(int linha[], int coluna[], int tam) {
    
}

int main() {
    int linha[10], coluna[10], tam;

    scanf("%d", &tam);

    inicializarJogo(linha, coluna, tam);

    return;
}